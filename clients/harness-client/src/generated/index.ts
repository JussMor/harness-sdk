// @harness/client — generated index re-exports.
//
// All types in this folder are produced by tygo from the Go SDK and event
// constants are produced by scripts/gen-events. They are committed so the
// package can be consumed without running the generator, but a CI guard
// (make client-types-check) ensures they stay in sync with sdk/*.go.

export * from "./events.js";
export * from "./types.js";
